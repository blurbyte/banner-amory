import styled from 'styled-components';

import { Logo as BaseLogo } from '../Icons';

const Logo = styled(BaseLogo)`
  position: absolute;
  z-index: 9999;
  top: 0;
  left: 2rem;
`;

export default Logo;
