import styled from 'styled-components';

const Grid = styled.div`
  display: grid;
  grid-gap: 1rem;
  grid-template-columns: repeat(10, 1fr);
  margin: 0;
  padding: 0;
  list-style: none;
`;

export default Grid;
